# ===== REQUIRED ENVIRONMENT VARIABLES =====
# These variables MUST be set for the application to start

# Database Connection (REQUIRED)
# Format: mongodb://[username:password@]host[:port]/database
# or mongodb+srv://[username:password@]cluster/database for Atlas
MONGODB_URI=mongodb://username:password@localhost:27017/superkafe_v2

# Database Connection Pool Configuration (OPTIONAL)
# These settings control the MongoDB connection pool behavior
# Adjust based on your application load and server resources
DB_MAX_POOL_SIZE=10                      # Maximum number of connections in pool (default: 10)
DB_MIN_POOL_SIZE=2                       # Minimum number of connections in pool (default: 2)
DB_SOCKET_TIMEOUT_MS=45000               # Socket timeout in milliseconds (default: 45000)
DB_SERVER_SELECTION_TIMEOUT_MS=5000      # Server selection timeout (default: 5000)
DB_HEARTBEAT_FREQUENCY_MS=10000          # Heartbeat frequency (default: 10000)
DB_MAX_IDLE_TIME_MS=60000                # Close idle connections after this time (default: 60000)
DB_WAIT_QUEUE_TIMEOUT_MS=10000           # Wait queue timeout (default: 10000)

# JWT Secret (REQUIRED)
# Must be at least 32 characters long for security
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-key-here-must-be-at-least-32-characters-long

# Server Port (OPTIONAL - defaults to 5001)
PORT=5001

# ===== OPTIONAL ENVIRONMENT VARIABLES =====
# These variables are optional and enable specific features

# API Key (OPTIONAL)
API_KEY=your-superkafe-api-key-here

# SMTP Configuration (for email verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Google OAuth
GOOGLE_CLIENT_ID=706624374984-qg1ueivbvougs0gff3842jkgvf9qurno.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# Frontend URL (Production & Development)
FRONTEND_URL=https://superkafe.com
FRONTEND_URL_DEV=http://localhost:5174


# Payment Gateway Configuration (Duitku)
PAYMENT_PROVIDER=duitku
DUITKU_MODE=production
DUITKU_MERCHANT_CODE=your_merchant_code_here
DUITKU_API_KEY=your_api_key_here
BACKEND_URL=https://superkafe.com
DUITKU_CALLBACK_URL=https://superkafe.com/api/payments/callback
